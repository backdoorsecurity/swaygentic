# Copy to run/xai.env (mode 0600) or export in your shell.
# Never commit the real key. Never put it in renderer JS or an unpacked extension.
XAI_API_KEY=xai-your-key-here

# Optional Leo compatibility proxy (Phase 1)
# LEO_PROXY_HOST=127.0.0.1
# LEO_PROXY_PORT=8787
